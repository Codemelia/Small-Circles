# Copy to .env and fill in if you want live OpenAI tool-calling.
# Without a key, the CLI runs in --mock mode automatically.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Set to 0 to hide [openai-debug] lines
OPENAI_DEBUG=1
